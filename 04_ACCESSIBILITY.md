# 접근성 설계·검수 기준

## 1. 기준 체계

### WCAG 2.2

웹 콘텐츠 접근성은 다음 4원칙으로 구성됩니다.

- **인식 가능(Perceivable)**: 정보를 감각적으로 인식할 대체 수단이 있다.
- **운용 가능(Operable)**: 키보드·포인터·보조기술로 조작할 수 있다.
- **이해 가능(Understandable)**: 내용과 동작이 명확하고 예측 가능하다.
- **견고함(Robust)**: 다양한 브라우저와 보조기술이 의미를 해석할 수 있다.

공식 개요: https://www.w3.org/WAI/standards-guidelines/wcag/

### KWCAG 2.2

한국 공공·기관 서비스는 한국형 웹 콘텐츠 접근성 지침 2.2와 해당 발주·인증 요건을 확인합니다.

공식 배포: https://www.rra.go.kr/ko/reference/kcsList_view.do?nb_seq=5247&nb_type=6

## 2. 자주 쓰는 수치 기준

| 항목 | 최소 기준·권장 |
|---|---|
| 일반 텍스트 대비 | WCAG AA 기준 4.5:1 이상 |
| 큰 텍스트 대비 | 3:1 이상 |
| 필요한 비텍스트 경계·아이콘·상태 대비 | 3:1 이상 |
| WCAG 2.2 AA 포인터 타깃 | 원칙적으로 24×24 CSS px 이상 또는 충분한 간격·예외 조건 |
| 실무 권장 터치 타깃 | 중요한 행동은 44×44 CSS px 이상을 우선 검토 |
| Apple 터치 타깃 | 일반적으로 44×44 pt 이상 |
| Material 터치 타깃 | 48×48 dp 이상 |
| 텍스트 확대 | 200%에서도 정보·기능 손실이 없어야 함 |
| 리플로 | 작은 뷰포트·확대에서 겹침과 불필요한 양방향 스크롤을 최소화 |

CSS px, pt, dp는 동일 단위가 아닙니다. 숫자만 맞추지 말고 플랫폼의 물리적 크기와 실제 입력 방식을 따릅니다.

## 3. 디자인 단계 체크리스트

### 색과 시각 정보

- 텍스트와 배경 대비를 실제 조합으로 검사합니다.
- 비활성 상태도 읽을 수 있어야 하며, 비활성 의미를 형태·문구로도 전달합니다.
- 성공·경고·오류를 색만으로 구분하지 않습니다.
- 그라디언트·이미지 위 텍스트는 가장 불리한 구간을 기준으로 검사합니다.
- 다크 모드도 별도 대비, 경계, 그림자 검사를 합니다.

### 키보드와 포커스

- 모든 핵심 기능이 키보드로 도달·실행 가능합니다.
- 포커스 순서가 시각적·논리적 순서와 일치합니다.
- 포커스 표시가 명확하고 sticky header·footer·overlay에 가려지지 않습니다.
- 모달은 포커스를 내부에서 관리하고 닫힌 뒤 원래 위치로 돌려보냅니다.
- Esc, Enter, Space, 방향키 등 패턴별 관습을 따릅니다.
- 키보드 함정이 없습니다.

### 터치와 포인터

- 작은 아이콘도 실제 상호작용 영역은 충분히 크게 만듭니다.
- 서로 다른 행동의 타깃을 너무 가깝게 배치하지 않습니다.
- 드래그만으로 가능한 기능에 클릭·버튼 대안을 제공합니다.
- hover만으로 필수 정보를 전달하지 않습니다.
- 터치, 마우스, 펜, 컨트롤러가 섞여도 핵심 기능을 잃지 않습니다.

### 텍스트와 구조

- 페이지·화면 제목이 명확합니다.
- 제목 계층이 시각과 의미 모두 일치합니다.
- 입력 필드는 영구적인 레이블을 가집니다.
- placeholder를 레이블 대신 쓰지 않습니다.
- 링크·버튼 문구만 읽어도 목적을 알 수 있습니다.
- 이미지 속 글자를 핵심 정보 전달 수단으로 쓰지 않습니다.
- 문서 언어와 부분 언어 변경을 올바르게 표시합니다.

### 오류·상태·알림

- 오류가 난 위치, 원인, 해결 행동을 함께 보여줍니다.
- 오류 후 사용자가 입력한 내용을 보존합니다.
- 상태 메시지는 시각 표시뿐 아니라 보조기술에도 전달될 구조를 가집니다.
- 로딩, 빈 상태, 오프라인, 권한 거부, 부분 성공을 설계합니다.
- 중요한 데이터 변경 행동은 검토·수정·취소 경로가 있습니다.

### 모션과 미디어

- 필수적이지 않은 모션은 줄이거나 끌 수 있습니다.
- `prefers-reduced-motion`에 대응할 대체 표현을 설계합니다.
- 자동 재생, 깜빡임, 과도한 시차 효과를 제한합니다.
- 영상은 자막, 필요한 경우 음성 설명과 대본을 제공합니다.
- 음성 안내만으로 핵심 행동을 전달하지 않습니다.

## 4. 검수 순서

1. **Figma·시안**: 대비, 타깃, 텍스트 계층, 상태, 포커스 주석
2. **프로토타입**: 흐름, 키보드 순서, 오류 회복, 확대, 회전
3. **개발 자동 검사**: WAVE, axe, Accessibility Insights
4. **개발 수동 검사**: 키보드만 사용, 브라우저 확대, 스크린리더 핵심 흐름
5. **실사용 테스트**: 다양한 장애, 연령, 기기, 환경에서 과업 수행
6. **회귀 검사**: 컴포넌트·콘텐츠·브라우저 업데이트 후 반복

자동 검사는 접근성 문제의 일부만 찾습니다. 자동 통과를 적합성 확정으로 취급하지 않습니다.

## 5. 권장 도구

| 도구 | 용도 | 주소 |
|---|---|---|
| WebAIM Contrast Checker | 색 대비 | https://webaim.org/resources/contrastchecker/ |
| WAVE | 웹페이지 1차 자동 점검 | https://wave.webaim.org/ |
| axe DevTools | 개발자 도구·자동화 | https://www.deque.com/axe/devtools/ |
| Accessibility Insights | 단계별 웹·Windows 검사 | https://accessibilityinsights.io/ |
| Stark | 디자인 단계 검사 | https://www.getstark.co/ |
| ARIA APG | 복합 위젯 행동·키보드 패턴 | https://www.w3.org/WAI/ARIA/apg/ |

## 6. 게임·키오스크·VR에 적용할 공통 기준

- 색 외의 상태 신호
- 자막과 음성 대체
- 텍스트 크기와 거리 가독성
- 입력 재매핑
- 반복 입력·길게 누르기 대안
- 모션·카메라 흔들림 조절
- 시간 제한 완화
- 컨트롤러 포커스의 명확성
- 키오스크 세션 초기화와 개인정보 삭제
- 도움 요청과 직원 호출 경로
